# Kali Backtracking Solver
A mobile-first network vulnerability pathfinder.

## Setup
1. Install Nmap: `pkg install nmap`
2. Install Python deps: `pip install -r requirements.txt`
3. Run Solver: `python solver.py`
